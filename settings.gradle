
include 'core'

include 'app'


def libs = [
        'picasso',
        'blur',
        'libphoneformat',
        'libsuperslim',
        'libtd',
        'libgif',
        'libwebp',
        'libopus',
        'provided_res',

]
for (l in libs){
        include l
        project(":$l").projectDir = new File(rootProject.projectDir, "libs/$l")
}

